<?php $this->restrict(); ?>
<div id="<?= $listId ?>" data-end-point="<?= $apiEndPoint ?>">
    <input type="hidden" name="csrf" value="<?= $csrf ?>">
    <h2><?= $title ?></h2>
    <hr>
    <div class="messages"></div>
    <div class="btn-group" role="group" aria-label="Basic example">
        <?php foreach ($actions as $action): ?>
            <button type="button" class="btn btn-secondary" onclick="document.location.href = '?q=<?= $action['link'] ?>'"><?= $action['text'] ?></button>
        <?php endforeach; ?>
    </div>
    <table class="table table-hover">
        <thead>
            <tr>
                <?php foreach ($columns as $column): ?>
                    <th scope="col" data-field="<?= $column['field'] ?>"><?= $column['text'] ?></th>
                <?php endforeach; ?>      
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>
<script>api.list.load('#<?= $listId ?>')</script>